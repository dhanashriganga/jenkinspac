def welcomenote(int a, int b) {
 println "welcome"
 return(a+b)
}

pipeline {
 agent any
 stages {
  stage("working with functions"){
   steps {
    script {
     result=welcomenote(10,20)
     println "my result is ${result}"
    }
   }
  }
 }
}
