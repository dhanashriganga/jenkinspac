pipeline {
 agent any
 stages{
  stage("working with variables"){
   steps {
    script {
      var1=20
      println "my var1 is ${var1}"
      println "my current build is ${currentBuild.number}"
    }
   }
  }
 }
}
