pipeline {
 agent any
 stages {
  stage("working with loops"){
   steps {
    script {
     mydata=[10,202,30]
     for(i in mydata) {
      println "my list values are ${i}"
     }
    }
   }
  }
 }
}
