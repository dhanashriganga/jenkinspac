def welcomenote(int a=20, int b=30) {
 println "welcome"
 return(a+b)
}

pipeline {
 agent any
 stages {
  stage("working with functions"){
   steps {
    script {
     println welcomenote()
     result=welcomenote(10,20)
     println "my result is ${result}"
    }
   }
  }
 }
}
